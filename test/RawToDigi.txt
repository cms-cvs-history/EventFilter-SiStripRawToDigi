process RawToDigiTest = {

# empty input service (empty events?), fire 2 events
source = EmptyInputService {untracked int32 maxEvents = 2}

# define modules to be used 
module prod = SiStripRawToDigiModule 
{
       string FedReadoutPath = "SLINK"
       int32  Verbosity = 3
}

#provide a scheduler path
path p1 = { prod }      

}

